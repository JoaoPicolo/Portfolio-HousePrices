# This folder contains the test codes for the project.

- The **data** folder contains the tests refering to data import and data cleaning

**Warning**
To run the tests use the command ```pytest tests``` from the **project folder**.